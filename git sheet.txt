Git is a control system which is used for tracking the changes in the code.

git -- version 
cd (change directory)
mkdir (make new directory)
clear
ls
ls -a (list hidden files)
git clone url
git status 
clear
git add <file name>
git commit -m "add a new paragraph" --> commit is a record of changes
Before tyring git push we need to use git push origin main 
push (upload local repo(local machine) content to remote(GitHub) repo) ---> git push origin main
init -- used to new git repo 

git init